# Codex team-sync server — v0.2 placeholder.
# The real Dockerfile will land alongside the server implementation.
FROM node:20-alpine
WORKDIR /app
COPY package.json ./
CMD ["node", "-e", "console.log('Codex team-sync server is not yet implemented.'); process.exit(1);"]
